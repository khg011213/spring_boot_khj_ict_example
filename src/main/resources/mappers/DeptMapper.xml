<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.ict.ex.mapper.DeptMapper">
    <select id="getList" resultType="edu.ict.ex.vo.DeptVO"> <!-- 리스트 타입이어도 괄호안에이쓴ㄴ VO를 적음 -->
        select * from dept
    </select>
</mapper>